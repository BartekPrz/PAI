<!DOCTYPE html>
<html>
    <head>
        <title>Turnieje badmintona</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css" integrity="sha384-HSMxcRTRxnN+Bdg0JdbxYKrThecOKuH5zCYotlSAcp1+c8xmyTe9GYg1l9a69psu"
		crossorigin="anonymous">
    </head>
    <body>
        <nav class="navbar navbar-default">
			<div class="container-fluid">
				<div class="navbar-header">
					<a class="navbar-brand" href="/">Turnieje badmintona</a>
				</div>
				<div class="collapse navbar-collapse">
					<ul class="nav navbar-nav navbar-right">
						<% if(!currentUser) { %>
							<li><a href="/login">Zaloguj się</a></li>
							<li><a href="/register">Zarejestruj się</a></li>
						<% } else { %>
							<li><a>Zalogowany jako <%= currentUser.dataValues.name%></a></li>
							<li><a href="/<%= currentUser.id %>/tournaments">Spotkania i turnieje</a></li> 
							<li><a href="/logout">Wyloguj się</a></li>
						<% } %>
					</ul>
				</div>
			</div>
		</nav>
		<div style="padding: 0;" class="container">
			<% if(error && error.length > 0) { %>
				<div class="alert alert-danger" role="alert">
					<%= error %>
				</div>
			<% } else if(success && success.length > 0) { %>
				<div class="alert alert-success" role="alert">
					<%= success %>
				</div>
			<% } %>
		</div>